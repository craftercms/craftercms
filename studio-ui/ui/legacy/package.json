{
  "private": true,
  "name": "@craftercms/studio-ui-legacy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "bic": {
    "skip": [
      "README.md",
      "CHANGE_LOG.md"
    ]
  },
  "scripts": {
    "build": "swc components -d ../../static-assets",
    "bom": "mkdir -p target && yarn cyclonedx --output-file target/bom.json"
  },
  "devDependencies": {
    "@babel/cli": "^8.0.4",
    "@swc/cli": "^0.8.0",
    "@swc/core": "^1.15.18",
    "build-if-changed": "^1.5.5"
  }
}
